Setup:

1. apt-get install python-dev python-pip
2. Install gtfsdb-load:
  a. Clone https://github.com/OpenTransitTools/gtfsdb
  b. Comment out `psycopg2` under `install_requires` in setup.py.
  c. python setup.py install --user
3. pip install --user pystache
4. cd ..
5. git clone https://github.com/jsha/lastbart
6. cd lastbart
7. PATH=~/.local/bin:$PATH make
8. firefox ./html/index.html
